{% extends 'base.html.twig' %}

{% block title %}Slaptažodžio keitimas{% endblock %}

{% block body %}
<header class="bg-profile">
    <div class="container">
        <div class="row align-items-end min-height">
            <div class="col-12">
                <h2 class="text-white pb-4">Slaptažodžio keitimas</h2>
            </div>
        </div>
    </div>
</header>
    <div class="container">
        <div class="row mt-5 py-3">
            <div class="col-12">
                <div class="mb-2">
                    <a class="btn btn-light" href="/profile">Atgal</a>
                </div>
            </div>
            <div class="col-12 py-4">
                {{ form_start(passwordForm) }}
                {{ form_widget(passwordForm) }}
                <button type="submit" class="btn btn-register" formnovalidate>Keisti</button>
                {{ form_end(passwordForm) }}
            </div>
        </div>
    </div>
{% endblock %}