{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<header class="bg-profile">
    <div class="container">
        <div class="row align-items-end min-height">
            <div class="col-12">
                <h2 class="text-white pb-4"></h2>
            </div>
        </div>
    </div>
</header>
<div class="container justify-content-center pt-lg-5 row">
    <div class="col-2">
        <a href="{{ path('profile') }}" class="btn btn-secondary">Rodyti profilį</a>
    </div>
    <div class="col-4">
        <a href="{{ path('admin_user_list') }}" class="btn btn-secondary">Rodyti vartotojų sąrašą</a>
    </div>
</div>
{% endblock %}
