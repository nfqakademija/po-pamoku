
<nav id="navbar" class="navbar navbar-expand-sm nav-trans fixed-top">
    <div class="container">
        <a href="/" class="navbar-brand logo">
            {# <img src="{{asset("build/logo.png")}}"> #}
            Po pamokų
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">Pagrindinis</a>
                </li>
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('login') }}" class="nav-link btn btn-green">Prisijungti</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('logout') }}" class="nav-link">Atsijungti</a>
                    </li>
                    <li class="nav-item">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('admin_profile') }}" class="nav-link">Admin</a>
                        {% else %}
                            <a href="{{ path('profile') }}" class="nav-link">{{ app.user.email }}</a>
                        {% endif %}
                        <a href="{{ path('fos_message_inbox') }}" class="nav-link">Inbox</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
